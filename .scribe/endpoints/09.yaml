name: Orders
description: |-

  APIs for managing customer orders after checkout is complete.
  Orders are created automatically when payment is confirmed via webhook
  (see **Checkout** and **Payment Webhooks** sections).

  ## Order Lifecycle

  ```
  pending → processing → completed
     ↓                      ↓
  cancelled              refunded (partial/full)
  ```

  | Status       | Meaning |
  |--------------|---------|
  | `pending`    | Order created, payment not yet confirmed (legacy v1 flow only) |
  | `processing` | Payment confirmed, keys being delivered |
  | `completed`  | All keys delivered to the customer |
  | `cancelled`  | Cancelled by customer or admin (wallet refunded if applicable) |
  | `refunded`   | Refund issued after completion |

  ## Payment Statuses

  | Status    | Meaning |
  |-----------|---------|
  | `pending` | Awaiting payment (legacy flow) |
  | `paid`    | Payment confirmed |
  | `failed`  | Payment attempt failed |
  | `refunded`| Full or partial refund issued |

  ## Post-Purchase Flow (Next.js)

  After checkout completes, use these endpoints:

  ```
  1. GET /my-orders                    → List all orders (with filters)
  2. GET /orders/{id}                  → Full order details (items, addresses, transactions)
  3. GET /orders/{id}/track            → Step-by-step timeline + per-item delivery status
  4. GET /my-keys/order/{id}           → Retrieve delivered license keys
  5. GET /orders/{id}/invoice          → Get invoice with PDF download link
  6. POST /orders/{id}/cancel          → Cancel a pending order
  7. GET /orders/{id}/reorder          → Pre-fill checkout from a previous order
  ```

  ## What Happens After Payment

  When a payment webhook is processed, the system automatically:
  1. Creates the order with `status: "processing"`, `payment_status: "paid"`
  2. Records seller earnings (with commission deduction)
  3. Assigns reserved license keys to the order items
  4. Generates an invoice (PDF)
  5. Dispatches auto-delivery (keys delivered to order items)
  6. Sends notifications to customer, seller(s), and admin
  7. Increments coupon usage (if a coupon was applied)

  Once all keys are delivered, the order status changes to `completed`.
endpoints:
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/my-orders
    metadata:
      custom: []
      groupName: Orders
      groupDescription: |-

        APIs for managing customer orders after checkout is complete.
        Orders are created automatically when payment is confirmed via webhook
        (see **Checkout** and **Payment Webhooks** sections).

        ## Order Lifecycle

        ```
        pending → processing → completed
           ↓                      ↓
        cancelled              refunded (partial/full)
        ```

        | Status       | Meaning |
        |--------------|---------|
        | `pending`    | Order created, payment not yet confirmed (legacy v1 flow only) |
        | `processing` | Payment confirmed, keys being delivered |
        | `completed`  | All keys delivered to the customer |
        | `cancelled`  | Cancelled by customer or admin (wallet refunded if applicable) |
        | `refunded`   | Refund issued after completion |

        ## Payment Statuses

        | Status    | Meaning |
        |-----------|---------|
        | `pending` | Awaiting payment (legacy flow) |
        | `paid`    | Payment confirmed |
        | `failed`  | Payment attempt failed |
        | `refunded`| Full or partial refund issued |

        ## Post-Purchase Flow (Next.js)

        After checkout completes, use these endpoints:

        ```
        1. GET /my-orders                    → List all orders (with filters)
        2. GET /orders/{id}                  → Full order details (items, addresses, transactions)
        3. GET /orders/{id}/track            → Step-by-step timeline + per-item delivery status
        4. GET /my-keys/order/{id}           → Retrieve delivered license keys
        5. GET /orders/{id}/invoice          → Get invoice with PDF download link
        6. POST /orders/{id}/cancel          → Cancel a pending order
        7. GET /orders/{id}/reorder          → Pre-fill checkout from a previous order
        ```

        ## What Happens After Payment

        When a payment webhook is processed, the system automatically:
        1. Creates the order with `status: "processing"`, `payment_status: "paid"`
        2. Records seller earnings (with commission deduction)
        3. Assigns reserved license keys to the order items
        4. Generates an invoice (PDF)
        5. Dispatches auto-delivery (keys delivered to order items)
        6. Sends notifications to customer, seller(s), and admin
        7. Increments coupon usage (if a coupon was applied)

        Once all keys are delivered, the order status changes to `completed`.
      subgroup: ''
      subgroupDescription: ''
      title: 'List my orders'
      description: |-
        Get a paginated list of orders for the authenticated user.
        Supports filtering by order status and payment status.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {YOUR_API_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters:
      status:
        custom: []
        name: status
        description: 'Filter by order status.'
        required: false
        example: pending
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      payment_status:
        custom: []
        name: payment_status
        description: 'Filter by payment status.'
        required: false
        example: paid
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      per_page:
        custom: []
        name: per_page
        description: 'Results per page (default 15, max 50).'
        required: false
        example: 10
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanQueryParameters:
      status: pending
      payment_status: paid
      per_page: 10
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"status":true,"message":"Success","data":{"current_page":1,"data":[],"total":0}}'
        headers: []
        description: ''
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 4|dQ9mJHW40BM01MiAKEF5AdQeftzpUuYnro5I8zxV21dde371'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: 'api/v1/orders/{order_id}'
    metadata:
      custom: []
      groupName: Orders
      groupDescription: |-

        APIs for managing customer orders after checkout is complete.
        Orders are created automatically when payment is confirmed via webhook
        (see **Checkout** and **Payment Webhooks** sections).

        ## Order Lifecycle

        ```
        pending → processing → completed
           ↓                      ↓
        cancelled              refunded (partial/full)
        ```

        | Status       | Meaning |
        |--------------|---------|
        | `pending`    | Order created, payment not yet confirmed (legacy v1 flow only) |
        | `processing` | Payment confirmed, keys being delivered |
        | `completed`  | All keys delivered to the customer |
        | `cancelled`  | Cancelled by customer or admin (wallet refunded if applicable) |
        | `refunded`   | Refund issued after completion |

        ## Payment Statuses

        | Status    | Meaning |
        |-----------|---------|
        | `pending` | Awaiting payment (legacy flow) |
        | `paid`    | Payment confirmed |
        | `failed`  | Payment attempt failed |
        | `refunded`| Full or partial refund issued |

        ## Post-Purchase Flow (Next.js)

        After checkout completes, use these endpoints:

        ```
        1. GET /my-orders                    → List all orders (with filters)
        2. GET /orders/{id}                  → Full order details (items, addresses, transactions)
        3. GET /orders/{id}/track            → Step-by-step timeline + per-item delivery status
        4. GET /my-keys/order/{id}           → Retrieve delivered license keys
        5. GET /orders/{id}/invoice          → Get invoice with PDF download link
        6. POST /orders/{id}/cancel          → Cancel a pending order
        7. GET /orders/{id}/reorder          → Pre-fill checkout from a previous order
        ```

        ## What Happens After Payment

        When a payment webhook is processed, the system automatically:
        1. Creates the order with `status: "processing"`, `payment_status: "paid"`
        2. Records seller earnings (with commission deduction)
        3. Assigns reserved license keys to the order items
        4. Generates an invoice (PDF)
        5. Dispatches auto-delivery (keys delivered to order items)
        6. Sends notifications to customer, seller(s), and admin
        7. Increments coupon usage (if a coupon was applied)

        Once all keys are delivered, the order status changes to `completed`.
      subgroup: ''
      subgroupDescription: ''
      title: 'Show order details'
      description: |-
        Get full details of a specific order including items, deliveries,
        transactions, billing address, wallet breakdown, tax details, and coupon info.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {YOUR_API_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      order_id:
        custom: []
        name: order_id
        description: 'The ID of the order.'
        required: true
        example: 17
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      order:
        custom: []
        name: order
        description: 'The order ID.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      order_id: 17
      order: 1
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"status":true,"message":"Success","data":{"order":{"id":1,"order_number":"000001","status":"pending"},"items":[],"transactions":[],"addresses":{}}}'
        headers: []
        description: ''
      -
        custom: []
        status: 403
        content: '{"status":false,"message":"You are not authorized to view this order."}'
        headers: []
        description: ''
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 4|dQ9mJHW40BM01MiAKEF5AdQeftzpUuYnro5I8zxV21dde371'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - POST
    uri: 'api/v1/orders/{order_id}/cancel'
    metadata:
      custom: []
      groupName: Orders
      groupDescription: |-

        APIs for managing customer orders after checkout is complete.
        Orders are created automatically when payment is confirmed via webhook
        (see **Checkout** and **Payment Webhooks** sections).

        ## Order Lifecycle

        ```
        pending → processing → completed
           ↓                      ↓
        cancelled              refunded (partial/full)
        ```

        | Status       | Meaning |
        |--------------|---------|
        | `pending`    | Order created, payment not yet confirmed (legacy v1 flow only) |
        | `processing` | Payment confirmed, keys being delivered |
        | `completed`  | All keys delivered to the customer |
        | `cancelled`  | Cancelled by customer or admin (wallet refunded if applicable) |
        | `refunded`   | Refund issued after completion |

        ## Payment Statuses

        | Status    | Meaning |
        |-----------|---------|
        | `pending` | Awaiting payment (legacy flow) |
        | `paid`    | Payment confirmed |
        | `failed`  | Payment attempt failed |
        | `refunded`| Full or partial refund issued |

        ## Post-Purchase Flow (Next.js)

        After checkout completes, use these endpoints:

        ```
        1. GET /my-orders                    → List all orders (with filters)
        2. GET /orders/{id}                  → Full order details (items, addresses, transactions)
        3. GET /orders/{id}/track            → Step-by-step timeline + per-item delivery status
        4. GET /my-keys/order/{id}           → Retrieve delivered license keys
        5. GET /orders/{id}/invoice          → Get invoice with PDF download link
        6. POST /orders/{id}/cancel          → Cancel a pending order
        7. GET /orders/{id}/reorder          → Pre-fill checkout from a previous order
        ```

        ## What Happens After Payment

        When a payment webhook is processed, the system automatically:
        1. Creates the order with `status: "processing"`, `payment_status: "paid"`
        2. Records seller earnings (with commission deduction)
        3. Assigns reserved license keys to the order items
        4. Generates an invoice (PDF)
        5. Dispatches auto-delivery (keys delivered to order items)
        6. Sends notifications to customer, seller(s), and admin
        7. Increments coupon usage (if a coupon was applied)

        Once all keys are delivered, the order status changes to `completed`.
      subgroup: ''
      subgroupDescription: ''
      title: 'Cancel order'
      description: |-
        Cancel a pending order. Refunds wallet balance if wallet was used,
        reverts seller earnings, reverses the transaction, and restores
        coupon usage if a coupon was applied. Only pending orders can be cancelled.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {YOUR_API_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      order_id:
        custom: []
        name: order_id
        description: 'The ID of the order.'
        required: true
        example: 17
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      order:
        custom: []
        name: order
        description: 'The order ID.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      order_id: 17
      order: 1
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"status":true,"message":"Order cancelled successfully."}'
        headers: []
        description: ''
      -
        custom: []
        status: 403
        content: '{"status":false,"message":"You are not authorized to cancel this order."}'
        headers: []
        description: ''
      -
        custom: []
        status: 422
        content: '{"status":false,"message":"Only pending orders can be cancelled."}'
        headers: []
        description: ''
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 4|dQ9mJHW40BM01MiAKEF5AdQeftzpUuYnro5I8zxV21dde371'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: 'api/v1/orders/{order_id}/track'
    metadata:
      custom: []
      groupName: Orders
      groupDescription: |-

        APIs for managing customer orders after checkout is complete.
        Orders are created automatically when payment is confirmed via webhook
        (see **Checkout** and **Payment Webhooks** sections).

        ## Order Lifecycle

        ```
        pending → processing → completed
           ↓                      ↓
        cancelled              refunded (partial/full)
        ```

        | Status       | Meaning |
        |--------------|---------|
        | `pending`    | Order created, payment not yet confirmed (legacy v1 flow only) |
        | `processing` | Payment confirmed, keys being delivered |
        | `completed`  | All keys delivered to the customer |
        | `cancelled`  | Cancelled by customer or admin (wallet refunded if applicable) |
        | `refunded`   | Refund issued after completion |

        ## Payment Statuses

        | Status    | Meaning |
        |-----------|---------|
        | `pending` | Awaiting payment (legacy flow) |
        | `paid`    | Payment confirmed |
        | `failed`  | Payment attempt failed |
        | `refunded`| Full or partial refund issued |

        ## Post-Purchase Flow (Next.js)

        After checkout completes, use these endpoints:

        ```
        1. GET /my-orders                    → List all orders (with filters)
        2. GET /orders/{id}                  → Full order details (items, addresses, transactions)
        3. GET /orders/{id}/track            → Step-by-step timeline + per-item delivery status
        4. GET /my-keys/order/{id}           → Retrieve delivered license keys
        5. GET /orders/{id}/invoice          → Get invoice with PDF download link
        6. POST /orders/{id}/cancel          → Cancel a pending order
        7. GET /orders/{id}/reorder          → Pre-fill checkout from a previous order
        ```

        ## What Happens After Payment

        When a payment webhook is processed, the system automatically:
        1. Creates the order with `status: "processing"`, `payment_status: "paid"`
        2. Records seller earnings (with commission deduction)
        3. Assigns reserved license keys to the order items
        4. Generates an invoice (PDF)
        5. Dispatches auto-delivery (keys delivered to order items)
        6. Sends notifications to customer, seller(s), and admin
        7. Increments coupon usage (if a coupon was applied)

        Once all keys are delivered, the order status changes to `completed`.
      subgroup: ''
      subgroupDescription: ''
      title: 'Track order'
      description: |-
        Get order tracking information with a step-by-step timeline
        (placed → paid → processing → delivered) and per-item delivery status.
        Delivery payload (license keys) is only exposed once delivery is complete.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {YOUR_API_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      order_id:
        custom: []
        name: order_id
        description: 'The ID of the order.'
        required: true
        example: 17
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      order:
        custom: []
        name: order
        description: 'The order ID.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      order_id: 17
      order: 1
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"status":true,"message":"Success","data":{"order_number":"000001","status":"processing","payment_status":"paid","timeline":[{"status":"order_placed","label":"Order Placed","completed":true,"date":"2026-02-28T00:00:00.000Z"}],"items":[]}}'
        headers: []
        description: ''
      -
        custom: []
        status: 403
        content: '{"status":false,"message":"You are not authorized to track this order."}'
        headers: []
        description: ''
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 4|dQ9mJHW40BM01MiAKEF5AdQeftzpUuYnro5I8zxV21dde371'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: 'api/v1/orders/{order_id}/invoice'
    metadata:
      custom: []
      groupName: Orders
      groupDescription: |-

        APIs for managing customer orders after checkout is complete.
        Orders are created automatically when payment is confirmed via webhook
        (see **Checkout** and **Payment Webhooks** sections).

        ## Order Lifecycle

        ```
        pending → processing → completed
           ↓                      ↓
        cancelled              refunded (partial/full)
        ```

        | Status       | Meaning |
        |--------------|---------|
        | `pending`    | Order created, payment not yet confirmed (legacy v1 flow only) |
        | `processing` | Payment confirmed, keys being delivered |
        | `completed`  | All keys delivered to the customer |
        | `cancelled`  | Cancelled by customer or admin (wallet refunded if applicable) |
        | `refunded`   | Refund issued after completion |

        ## Payment Statuses

        | Status    | Meaning |
        |-----------|---------|
        | `pending` | Awaiting payment (legacy flow) |
        | `paid`    | Payment confirmed |
        | `failed`  | Payment attempt failed |
        | `refunded`| Full or partial refund issued |

        ## Post-Purchase Flow (Next.js)

        After checkout completes, use these endpoints:

        ```
        1. GET /my-orders                    → List all orders (with filters)
        2. GET /orders/{id}                  → Full order details (items, addresses, transactions)
        3. GET /orders/{id}/track            → Step-by-step timeline + per-item delivery status
        4. GET /my-keys/order/{id}           → Retrieve delivered license keys
        5. GET /orders/{id}/invoice          → Get invoice with PDF download link
        6. POST /orders/{id}/cancel          → Cancel a pending order
        7. GET /orders/{id}/reorder          → Pre-fill checkout from a previous order
        ```

        ## What Happens After Payment

        When a payment webhook is processed, the system automatically:
        1. Creates the order with `status: "processing"`, `payment_status: "paid"`
        2. Records seller earnings (with commission deduction)
        3. Assigns reserved license keys to the order items
        4. Generates an invoice (PDF)
        5. Dispatches auto-delivery (keys delivered to order items)
        6. Sends notifications to customer, seller(s), and admin
        7. Increments coupon usage (if a coupon was applied)

        Once all keys are delivered, the order status changes to `completed`.
      subgroup: ''
      subgroupDescription: ''
      title: 'Get order invoice'
      description: |-
        Retrieve the invoice for a paid order including totals, status,
        and a download URL for the PDF. Only available after payment is confirmed.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {YOUR_API_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      order_id:
        custom: []
        name: order_id
        description: 'The ID of the order.'
        required: true
        example: 17
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      order:
        custom: []
        name: order
        description: 'The order ID.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      order_id: 17
      order: 1
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"status":true,"message":"Success","data":{"invoice_number":"INV-000001","status":"paid","subtotal":29.99,"tax_total":2.40,"discount_total":5.00,"grand_total":27.39,"currency":"USD","issued_at":"2026-02-28T00:00:00.000Z","paid_at":"2026-02-28T00:00:00.000Z","download_url":"https://example.com/invoices/1/download"}}'
        headers: []
        description: ''
      -
        custom: []
        status: 403
        content: '{"status":false,"message":"Unauthorized."}'
        headers: []
        description: ''
      -
        custom: []
        status: 404
        content: '{"status":false,"message":"Invoice not available yet. Payment must be confirmed first."}'
        headers: []
        description: ''
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 4|dQ9mJHW40BM01MiAKEF5AdQeftzpUuYnro5I8zxV21dde371'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: 'api/v1/orders/{order_id}/reorder'
    metadata:
      custom: []
      groupName: Orders
      groupDescription: |-

        APIs for managing customer orders after checkout is complete.
        Orders are created automatically when payment is confirmed via webhook
        (see **Checkout** and **Payment Webhooks** sections).

        ## Order Lifecycle

        ```
        pending → processing → completed
           ↓                      ↓
        cancelled              refunded (partial/full)
        ```

        | Status       | Meaning |
        |--------------|---------|
        | `pending`    | Order created, payment not yet confirmed (legacy v1 flow only) |
        | `processing` | Payment confirmed, keys being delivered |
        | `completed`  | All keys delivered to the customer |
        | `cancelled`  | Cancelled by customer or admin (wallet refunded if applicable) |
        | `refunded`   | Refund issued after completion |

        ## Payment Statuses

        | Status    | Meaning |
        |-----------|---------|
        | `pending` | Awaiting payment (legacy flow) |
        | `paid`    | Payment confirmed |
        | `failed`  | Payment attempt failed |
        | `refunded`| Full or partial refund issued |

        ## Post-Purchase Flow (Next.js)

        After checkout completes, use these endpoints:

        ```
        1. GET /my-orders                    → List all orders (with filters)
        2. GET /orders/{id}                  → Full order details (items, addresses, transactions)
        3. GET /orders/{id}/track            → Step-by-step timeline + per-item delivery status
        4. GET /my-keys/order/{id}           → Retrieve delivered license keys
        5. GET /orders/{id}/invoice          → Get invoice with PDF download link
        6. POST /orders/{id}/cancel          → Cancel a pending order
        7. GET /orders/{id}/reorder          → Pre-fill checkout from a previous order
        ```

        ## What Happens After Payment

        When a payment webhook is processed, the system automatically:
        1. Creates the order with `status: "processing"`, `payment_status: "paid"`
        2. Records seller earnings (with commission deduction)
        3. Assigns reserved license keys to the order items
        4. Generates an invoice (PDF)
        5. Dispatches auto-delivery (keys delivered to order items)
        6. Sends notifications to customer, seller(s), and admin
        7. Increments coupon usage (if a coupon was applied)

        Once all keys are delivered, the order status changes to `completed`.
      subgroup: ''
      subgroupDescription: ''
      title: Reorder
      description: |-
        Pre-fill checkout data from a previous order. Returns available items
        with current stock levels, flags unavailable items, and includes
        the saved billing address. Use the returned data to submit a new order.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {YOUR_API_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      order_id:
        custom: []
        name: order_id
        description: 'The ID of the order.'
        required: true
        example: 17
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      order:
        custom: []
        name: order
        description: 'The original order ID.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      order_id: 17
      order: 1
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"status":true,"message":"All items are available for reorder.","data":{"items":[{"seller_offer_id":5,"quantity":2,"available_stock":10,"product":{"id":1,"title":"Windows 11 Pro","price":29.99}}],"unavailable":[],"billing":{"name":"John Doe","email":"john@example.com"},"currency":"USD","payment_method":"stripe"}}'
        headers: []
        description: ''
      -
        custom: []
        status: 403
        content: '{"status":false,"message":"Unauthorized."}'
        headers: []
        description: ''
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 4|dQ9mJHW40BM01MiAKEF5AdQeftzpUuYnro5I8zxV21dde371'
    controller: null
    method: null
    route: null
