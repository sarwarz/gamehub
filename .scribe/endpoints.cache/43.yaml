## Autogenerated by Scribe. DO NOT MODIFY.

name: 'Order Keys'
description: |-

  APIs for buyers to access their purchased license keys
  and report issues with invalid or already-redeemed keys.

  ## When Keys Become Available

  Keys are delivered automatically after payment is confirmed.
  The typical timeline is:

  1. Payment confirmed via webhook → order created
  2. `DispatchAutoDelivery` listener assigns keys from the seller's key pool
  3. Keys appear in `GET /my-keys` with `status: "delivered"`

  This usually happens within **seconds** of payment confirmation.

  ## Retrieving Keys

  ```
  GET /my-keys                    → All keys across all orders
  GET /my-keys/order/{order_id}   → Keys for a specific order
  ```

  Each delivery contains a `keys` array with the actual license key strings.
  Keys are only visible after `status` is `"delivered"`.

  ## Reporting Bad Keys

  If a customer receives an invalid or already-redeemed key:

  ```
  POST /my-keys/deliveries/{delivery_id}/report
  body: { key_index: 0, reason: "Key already redeemed" }
  ```

  This automatically creates a high-priority support ticket for investigation.
  The `key_index` refers to the position in the `keys` array (0-based).
endpoints:
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/my-keys
    metadata:
      custom: []
      groupName: 'Order Keys'
      groupDescription: |-

        APIs for buyers to access their purchased license keys
        and report issues with invalid or already-redeemed keys.

        ## When Keys Become Available

        Keys are delivered automatically after payment is confirmed.
        The typical timeline is:

        1. Payment confirmed via webhook → order created
        2. `DispatchAutoDelivery` listener assigns keys from the seller's key pool
        3. Keys appear in `GET /my-keys` with `status: "delivered"`

        This usually happens within **seconds** of payment confirmation.

        ## Retrieving Keys

        ```
        GET /my-keys                    → All keys across all orders
        GET /my-keys/order/{order_id}   → Keys for a specific order
        ```

        Each delivery contains a `keys` array with the actual license key strings.
        Keys are only visible after `status` is `"delivered"`.

        ## Reporting Bad Keys

        If a customer receives an invalid or already-redeemed key:

        ```
        POST /my-keys/deliveries/{delivery_id}/report
        body: { key_index: 0, reason: "Key already redeemed" }
        ```

        This automatically creates a high-priority support ticket for investigation.
        The `key_index` refers to the position in the `keys` array (0-based).
      subgroup: ''
      subgroupDescription: ''
      title: 'List my keys'
      description: "Get all delivered keys for the authenticated user's completed orders."
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {YOUR_API_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters:
      order_id:
        custom: []
        name: order_id
        description: 'Filter by specific order.'
        required: false
        example: 5
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      per_page:
        custom: []
        name: per_page
        description: 'Results per page (default 15).'
        required: false
        example: 10
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanQueryParameters:
      order_id: 5
      per_page: 10
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"status":true,"message":"Keys fetched","data":{"current_page":1,"data":[],"total":0}}'
        headers: []
        description: ''
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 4|dQ9mJHW40BM01MiAKEF5AdQeftzpUuYnro5I8zxV21dde371'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: 'api/v1/my-keys/order/{order}'
    metadata:
      custom: []
      groupName: 'Order Keys'
      groupDescription: |-

        APIs for buyers to access their purchased license keys
        and report issues with invalid or already-redeemed keys.

        ## When Keys Become Available

        Keys are delivered automatically after payment is confirmed.
        The typical timeline is:

        1. Payment confirmed via webhook → order created
        2. `DispatchAutoDelivery` listener assigns keys from the seller's key pool
        3. Keys appear in `GET /my-keys` with `status: "delivered"`

        This usually happens within **seconds** of payment confirmation.

        ## Retrieving Keys

        ```
        GET /my-keys                    → All keys across all orders
        GET /my-keys/order/{order_id}   → Keys for a specific order
        ```

        Each delivery contains a `keys` array with the actual license key strings.
        Keys are only visible after `status` is `"delivered"`.

        ## Reporting Bad Keys

        If a customer receives an invalid or already-redeemed key:

        ```
        POST /my-keys/deliveries/{delivery_id}/report
        body: { key_index: 0, reason: "Key already redeemed" }
        ```

        This automatically creates a high-priority support ticket for investigation.
        The `key_index` refers to the position in the `keys` array (0-based).
      subgroup: ''
      subgroupDescription: ''
      title: 'Show keys for a specific order'
      description: 'Get all delivered keys for a specific order the user owns.'
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {YOUR_API_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      order:
        custom: []
        name: order
        description: 'Order ID.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      order: 1
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"status":true,"message":"Order keys fetched","data":[]}'
        headers: []
        description: ''
      -
        custom: []
        status: 403
        content: '{"status":false,"message":"Unauthorized"}'
        headers: []
        description: ''
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 4|dQ9mJHW40BM01MiAKEF5AdQeftzpUuYnro5I8zxV21dde371'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - POST
    uri: 'api/v1/my-keys/deliveries/{delivery}/report'
    metadata:
      custom: []
      groupName: 'Order Keys'
      groupDescription: |-

        APIs for buyers to access their purchased license keys
        and report issues with invalid or already-redeemed keys.

        ## When Keys Become Available

        Keys are delivered automatically after payment is confirmed.
        The typical timeline is:

        1. Payment confirmed via webhook → order created
        2. `DispatchAutoDelivery` listener assigns keys from the seller's key pool
        3. Keys appear in `GET /my-keys` with `status: "delivered"`

        This usually happens within **seconds** of payment confirmation.

        ## Retrieving Keys

        ```
        GET /my-keys                    → All keys across all orders
        GET /my-keys/order/{order_id}   → Keys for a specific order
        ```

        Each delivery contains a `keys` array with the actual license key strings.
        Keys are only visible after `status` is `"delivered"`.

        ## Reporting Bad Keys

        If a customer receives an invalid or already-redeemed key:

        ```
        POST /my-keys/deliveries/{delivery_id}/report
        body: { key_index: 0, reason: "Key already redeemed" }
        ```

        This automatically creates a high-priority support ticket for investigation.
        The `key_index` refers to the position in the `keys` array (0-based).
      subgroup: ''
      subgroupDescription: ''
      title: 'Report a bad key'
      description: |-
        Report a delivered key as invalid or already used.
        Creates a support ticket automatically for investigation.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {YOUR_API_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      delivery:
        custom: []
        name: delivery
        description: 'Delivery ID.'
        required: true
        example: 1
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      delivery: 1
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters:
      key_index:
        custom: []
        name: key_index
        description: 'Index of the bad key in the keys array.'
        required: true
        example: 0
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      reason:
        custom: []
        name: reason
        description: 'Reason for reporting.'
        required: true
        example: 'Key already redeemed'
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanBodyParameters:
      key_index: 0
      reason: 'Key already redeemed'
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: '{"status":true,"message":"Key reported. A support ticket has been created."}'
        headers: []
        description: ''
      -
        custom: []
        status: 404
        content: '{"status":false,"message":"Delivery not found"}'
        headers: []
        description: ''
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 4|dQ9mJHW40BM01MiAKEF5AdQeftzpUuYnro5I8zxV21dde371'
    controller: null
    method: null
    route: null
